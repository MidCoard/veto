project:
  name: veto
  version: 1.0.98
  description: Zero-Trust Cloud-Edge Agent Backend
  longDescription: |
    Project Veto is the core backend engine for the enterprise-grade, multi-tenant Agent Client.
    It operates on a Cloud-Edge Collaborative Topology, providing critical security and orchestration layers.
  authors:
    - Focess
  license: AGPL-3.0-only
  links:
    homepage: https://github.com/MidCoard/veto
  java:
    version: 25
    groupId: top.focess
    artifactId: veto

release:
  github:
    owner: MidCoard
    overwrite: true
    sign: true

distributions:
  veto:
    type: JAVA_BINARY
    executable:
      name: veto
    artifacts:
      - path: artifacts/veto-jar/veto-core-{{projectVersion}}.jar
    brew:
      active: NEVER
      formulaName: veto
    scoop:
      active: ALWAYS

  veto-native:
    type: NATIVE_IMAGE
    executable:
      name: veto
    artifacts:
      - path: artifacts/veto-ubuntu-latest/veto
        platform: linux-x86_64
      - path: artifacts/veto-windows-latest/veto.exe
        platform: windows-x86_64
