---
version: 2
id: secret-detection
requires: [text]
---
List every secret (API key, token, password, private key, credential) in the text as a JSON array of the exact substrings, e.g. ["abc","def"]. Reply with JSON only.

{{text}}
