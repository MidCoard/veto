---
version: 2
id: builtin-citation-correction
requires: [id, index, selected, count, items]
---
Citation {{id}} does not occur in message_index {{index}}.
@if nonempty(selected)
That index selects an input item with role {{selected}}.
@else
That index is outside the input.
@endif
This request contains {{count}} non-system input items. Copy a short exact passage from visible evidence and omit message_index so the runtime can locate it. Use an explicit index only from source candidates supplied by a citation diagnostic; do not guess or count messages. Preserve punctuation, URLs, and whitespace. Correct both the citation source and its cite: link.

Input order (system excluded):
@for item in items
{{item.index}}:{{item.role}}
@if item.toolCall
(tool call)
@endif
@endfor
