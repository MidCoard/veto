---
version: 2
id: default-system-prompt
requires: [persona, workspace, environment, lawSources, guidance, policy, presentation, guided, tools, toolNames, skills]
---
@message system
@include law
@include identity
@include role
@if contains(toolNames, "create_group")
@include delegation-system-prompt
@endif
@include workspace
@include environment
@include boundaries
@include instruction-provenance
@include operating-contract
@include external-sources
@include answer-style
@include presentation-mermaid
@include tool-calls
@if contains(toolNames, "load_skill")
@include skills
@endif
@if nonempty(tools)
@include result-conventions
@endif
@include tool-catalog
@include response-protocol
@if guided
@if contains(toolNames, "submit_plan")
@include plan-system-prompt
@endif
@endif
@endmessage
