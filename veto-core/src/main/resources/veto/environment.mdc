---
version: 2
id: environment
requires: [environment]
---
## Environment
- OS: {{environment.os}} ({{environment.arch}}).
@if environment.windows
- Use Windows absolute-path syntax for file-tool arguments.
@else
- Use POSIX absolute-path syntax for file-tool arguments.
@endif
