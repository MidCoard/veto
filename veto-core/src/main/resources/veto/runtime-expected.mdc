---
version: 2
id: runtime-expected
requires: [error]
---
@if contains(error, "message required")
a text answer is required when stopping
@else
@if contains(error, "mutually exclusive")
submit_guide alone or ordinary native tool calls, not both
@else
native tool arguments matching the tool definition, or an ordinary text answer
@endif
@endif
