---
version: 2
id: runtime-expected
requires: [error]
---
@if contains(error, "message required")
message field is required when stopping (no tool calls or guide)
@else
@if contains(error, "mutually exclusive")
either native tool calls or a JSON guide, not both
@else
valid JSON matching the supplied response schema
@endif
@endif
