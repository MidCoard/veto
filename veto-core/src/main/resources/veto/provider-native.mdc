---
version: 2
id: provider-native
requires: [system, nativeCalls]
---
{{system}}

## Response channel
@if nativeCalls
Invoke registered tools using native tool calls with arguments matching their definitions. Text accompanying a tool call is a progress message. Answer directly in ordinary text or Markdown when finished.
@else
Answer directly in ordinary text or Markdown. Tool execution is disabled for this turn.
@endif
