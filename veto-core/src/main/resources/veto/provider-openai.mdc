---
version: 2
id: provider-openai
requires: [system, schema]
---
{{system}}

For the JSON text channel, return a valid JSON object matching this schema:
{{json(schema)}}
The provider response channel instructions above govern tool execution.
