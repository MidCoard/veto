---
version: 2
id: provider-openai
requires: [system, schema]
---
{{system}}

For the JSON text channel, return a valid JSON object matching this schema:
{{json(schema)}}
Native tool calls remain preferred when enabled above; they do not need this JSON envelope.
