---
version: 2
id: group-disband-brief
requires: [group]
---
Delegation complete. You led a group to the following outcome.
@if group.available
Group id: {{group.id}}
Original brief:
@if nonempty(group.brief)
{{group.brief}}
@else
(unspecified)
@endif
Node outcomes:
@for node in group.nodes
@include group-node
@endfor
You are back in single-agent autonomous mode. Continue from here.
@else
(group record no longer available)
@endif
